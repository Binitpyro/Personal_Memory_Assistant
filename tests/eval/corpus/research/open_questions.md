# Open questions

Does holding the seed fixed across a sequence actually help continuity, or
does it just make the errors correlated in a way that reads worse?

Is there any case where we want the higher octave counts, or has that
recommendation been carried forward from a context that no longer applies?

How much of the startup cost is import time versus actual initialisation?
