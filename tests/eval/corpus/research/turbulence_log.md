# Experiment log: second act tower

Ran eleven variants over two days. The ones that read best on the plate were
not the ones with the highest frequency content, which surprised me. Detail
that survives motion blur is coarser than detail that looks good on a frame
grab.

Settled on a mid range setting and moved the remaining budget into
resolution instead. Note for later: judge these on moving frames only.
