"""Retrieval evaluation harness.

Opt-in: everything here is gated behind the `eval` pytest marker because it
indexes a real corpus with real embedding and reranker models. See
`tests/test_eval_retrieval.py`.
"""
